# プロジェクト全体の指示

## 目的と構成

- Raspberry Pi 3BにUSB接続したカメラをTailscale内から操作するWebアプリとして保つ。
- バックエンドはPython 3、FastAPI、Uvicornを使用する。
- フロントエンドはJinja2、HTML、CSS、Vanilla JavaScriptを使用し、明示的な依頼がない限りNode.jsやビルド工程を追加しない。
- 本番環境ではsystemdでアプリを常駐させ、Tailscale ServeをHTTPSの入口にする。
- Cloudflare、Caddy、Tailscale Funnel、ルーターのポート開放は使用しない。

## ドキュメントの責務

- `README.md` は人間向けの概要、主要機能、最短の起動方法に留める。
- 配備や運用の詳細は `docs/` に置き、READMEへ重複させない。
- Tailscale固有の設定は `docs/tailscale-setup.md` に集約する。
- `AGENTS.md` はGit管理し、秘密情報、実IP、tailnet名、認証キーなどは記載しない。
- サブディレクトリ固有の規約が増えた場合だけ、そのディレクトリへ追加の `AGENTS.md` を置く。ルート規約の単純な複製は作らない。

## 推奨ディレクトリ構成

```text
app/
├── main.py
├── api/
├── services/
├── templates/
└── static/
    ├── css/
    └── js/
data/
└── photos/
docs/
systemd/
tests/
```

- FastAPIのルート定義は薄く保ち、撮影や保存期間管理は `app/services/` に分離する。
- HTMLテンプレートは `app/templates/`、CSSとJavaScriptは `app/static/` に置く。
- 撮影画像は `data/photos/` に保存し、Git管理しない。
- systemd unitなど配備用ファイルは `systemd/` に置く。

## API設計

- 撮影は副作用を伴うため `POST /api/capture` とする。
- 一覧取得は `GET /api/photos`、画像取得は `GET /photos/{photo_id}` とする。
- クライアントから任意のファイルパスや撮影コマンドを受け取らない。
- ファイル名はサーバー側で生成し、原則として `YYYYMMDD-HHMMSS` と衝突防止要素を使う。
- APIエラーは一貫したJSON形式で返し、内部パスやコマンド出力を利用者へ露出しない。
- `/docs` とOpenAPIの公開範囲は、Tailscale内限定であることを前提に判断する。

## カメラ制御

- カメラ実装はサービス層へ抽象化し、テスト用実装へ交換可能にする。
- 利用可能な撮影コマンドを実機で確認してから実装を確定する。
- USBカメラは `/dev/video0` と `fswebcam` を既定とし、デバイス、解像度、コマンドは環境変数で変更可能にする。
- 外部コマンドは引数配列で実行し、`shell=True` を使用しない。
- 非同期サブプロセスまたはスレッドオフロードを使い、FastAPIのイベントループをブロックしない。
- 撮影処理にプロセス内ロックを設け、同時実行を防止する。
- Pi 3Bを前提に、Uvicornは原則1ワーカーとし、高解像度変換や常時動画変換を避ける。

## セキュリティ

- アプリは原則 `127.0.0.1:8000` で待ち受ける。
- 外部到達性を追加する変更では、Tailscale内限定という前提を維持する。
- Tailscale認証キー、秘密鍵、Cookie、トークン、実際のtailnet名をコミットしない。
- Tailscale Serveから転送される識別ヘッダーを利用する場合、アプリへ直接到達できない構成を維持する。
- 撮影APIには連打制限を設ける。
- 保存枚数または保存期間に上限を設け、SDカードの枯渇を防止する。
- ファイル参照ではIDから管理対象ファイルを解決し、パストラバーサルを防止する。
- systemdサービスはrootではなく専用の一般ユーザーで実行する。

## 実装と依存関係

- Python本体、仮想環境、依存関係、ロックファイルは `uv` で管理する。
- 依存関係の正本は `pyproject.toml`、再現可能な解決結果は `uv.lock` とし、両方をGit管理する。
- 開発用依存関係はuvのdependency group `dev` に分離する。
- 通常の開発コマンドは `uv sync`、`uv run pytest`、`uv run ruff check .` を使用する。
- `pip install`、手作業の `.venv` 作成、`requirements.txt` との二重管理を追加しない。
- Pythonコードには型注釈を付ける。
- 設定値は環境変数または設定ファイルから読み込み、安全な既定値を用意する。
- 依存関係は必要最小限にし、追加理由が説明できないパッケージを導入しない。
- OSコマンドやカメラがない開発環境でもテストできるよう、カメラサービスを差し替え可能にする。
- 日時を扱う際はタイムゾーンを明示し、保存用IDと表示時刻を混同しない。
- Python 3.9以上を対象とし、サポート切れOSのために古いFastAPIへ固定しない。

## テストと確認

変更内容に応じて、最低限次を確認する。

1. Pythonの構文・静的検査が成功する。
2. 自動テストが成功する。
3. カメラサービスの正常系、コマンド失敗、タイムアウト、同時撮影拒否をテストする。
4. APIが任意のファイルパスを参照できないことをテストする。
5. 撮影データと秘密情報がGitの追跡対象になっていないことを確認する。
6. 実機変更では、撮影、画像表示、再起動後のsystemd起動、Tailscale経由の接続を確認する。

実機やカメラを利用できない環境では、実機確認を未実施と明記し、モックを使ったテスト結果と区別する。

現在の実装状況と次に行う作業は `docs/development.md` を参照する。
